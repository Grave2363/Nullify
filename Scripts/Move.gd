extends Node

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
# use if a tween is used to move something 
func move(target):
	# this is commented out to prevent an error as this at the point of making because of errors 
	#var move_tween = get_node("move_tween")
	#move_tween.interpolate_property(self, "position", position , target, 2, Tween.TRANS_QUINT, Tween.EASE_OUT)
	#move_tween.start()
	pass
